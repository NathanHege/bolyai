#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

int main()
{
/*
    int a , b;
    cout<<"Hany almat szedett peti? ";
    cin>>a;
    cout<<"Joci hany almat szedett? ";
    cin>>b;
    if (a>b)
        cout<<"peti tobb almat szedett jocinal ";
    else
        cout<<"nem szedett tobbet ";
    return 0;
*/
/*
    int szam;
    cout<<"irj be egy szamot: ";
    cin>>szam;
    if (szam>0)
        cout<<"a szam abszolutto erteke: "<<szam*(-1)<<"\n";
    else
        cout<<"Itt a szep gyonyoru szama: "<<szam*1<<"\n";
    if (szam %2==0)
        cout<<"paros";
    else
        cout<<"paratlan";
*/
/*
    int a, b, c;
    cout<<"kerem az a oldalt: \n";
    cin>>a;
    cout<<"kerem a b oldalt: \n";
    cin>>b;
    cout<<"kerem a c oldalt: \n";
    cin>>c;
    if (a==b&&b==c)
        cout<<"Ez egy egyenlo oldalu haromszog\n";
    else if (a==b && b!=c || a==c && c!=b || b==c && c!=a)
        cout<<"Ez egy egyenlo szaru haromszog\n";
    else if (a!=b && b!=c)
        cout<<"ez egy altalanos haromszog";
    */
/*
    double  x,f;
    cout<<"add me az x erteket: ";
    cin>>x;
    if(x<0)
        f=pow(x,2);
    else if (x==0)
        f=x;
    else
        f= sin(x);
    cout<<"nem tudom mit kell itt kiirni: "<<f<<"\n";
*/
/*
    double x, y, z, a, b;
    cout << "Adj meg harom valos szamot: \n";
    cin>>x>>y>>z;
    cout<<"add meg az also hatarat az intervallumnak: \n";
    cin>>a;
    cout<<"add meg a felso hatarat az intervallumnak: \n";
    cin>>b;

    if(x>=a && x<=b)
        cout<<"az intervallumban benne van az x\n";
    else
        cout<<"az intervallumban nincs benne az x\n";

    if(z>=a && z<=b)
        cout<<"az intervallumban benne van a z\n";
    else
        cout<<"az intervallumban nincs benne a z\n";

    if(y>=a && y<=b)
        cout<<"az intervallumban benne van az y\n";
    else
        cout<<"az intervallumban nincs benne az y\n";
        */

}
